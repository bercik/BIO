#INCLUDE("boreq_lib.biom")

def onSTART()
   splash(architectures = CREATE_TUPLE("x86"), program_name="Fizz Buzz")

   FOR
   (
      ASSIGN_LOCAL(i, 0),
      LS(i, 100),
      fizz_buzz(i),
      INC(i)
   )
end
