def onSTART()
   AS_LOC(i, 10)
   AS_LOC(j, i)
   PRINT(i, j, "\n")
   AS_LOC(i, 20)
   PRINT(i, j, "\n")

   AS_GLOB(x, "global x")
   AS_LOC(x, "local onSTART x")
   foo()
   PRINT(GET_LOC(x), GET_GLOB(x), "\n")

   PRINT(GET_GLOB(z), "\n")
end

def foo()
   AS_LOC(x, "local x")
   PRINT(GET_LOC(x), GET_GLOB(x), "\n")
   PRINT(x, GET_GLOB(x), "\n")
   AS_GLOB(x, "foo global x")
end
