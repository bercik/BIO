def onSTART()
   FOR
   (
      ASSIGN_LOCAL(i, 0),
      true,
      CALL2
      (
         PRINT(ADD(TO_STR(i), " jest ")),
         IF(EQ(REST(i, 2), 0), PRINT("parzysta"), PRINT("nieparzysta"))
      ),
      INC(i)
   )
end
