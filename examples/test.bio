#INCLUDE("<stdlib/iter_functions.biom>")

def onSTART()
   AS_LOC(tup, CR_TUP("abc", "abba", "AbC", "def", "acbd", "a"))
   AS_LOC(result, GET_ARRAY_LIST(FOREACH(tup, retListWithStringsStartsWith)))
   PRINTLN(result)
   PRINT_STACK_TRACE(result)
end
