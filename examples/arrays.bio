def onSTART()
   ASSIGN_LOCAL_TABLE(tab, 10) % tablica tab 10 elementów None
   SET(tab, 5, 20) % tab[5] = 20
   PRINT(GET(tab, 5)) % 20

   FOR
   (
      ASSIGN_LOCAL(i, 0), % i = 0
      LS(i, 10), % i < 10
      PRINTLN(GET(tab, i)), % println(tab[i])
      DN()
   ) 
end
