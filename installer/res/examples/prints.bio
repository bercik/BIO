def onSTART()
   foo(retName(), retAge())
end

def retName()
   ASSIGN_LOCAL(name, "Borek")
   RETURN(name)
end

def retAge()
   ASSIGN_GLOBAL(age, 22)
end

def foo(name, age)
   PRINT("IS_LOCAL:", IS_LOCAL(name), "\n")
   PRINT("IS_GLOBAL:", IS_GLOBAL(age2), "\n")
   PRINT("Your name is", name, "and you are", GET_GLOBAL(age), "years old\n")
end
