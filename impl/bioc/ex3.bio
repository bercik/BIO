def onSTART()
   PRINT(fact(10))
end

% liczy silnię z podanej liczby
def fact(n)
   ASSIGN_LOCAL(res, 1)
   FOR
   (
      ASSIGN_LOCAL(i, 2),
      LE(i, n),
      ASSIGN_LOCAL(res, MUL(res, i)),
      INC(i)
   )
   RETURN(res)
end
