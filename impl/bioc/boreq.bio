def onSTART()
   FOR
   (
      ASSIGN_LOCAL(i, 0),
      LS(i, 100),
      IF 
      (  
         EQ(REST(i, 2), 0), 
         PRINT("FIZZ"), 
         IF
         (
            EQ(REST(i, 5), 0),
            PRINT("FIZZBUZZ"),
            PRINT("BUZZ")
         )
      ),
      INC(i)
   )
end

def butt(penis)
   PRINT(penis)
end
